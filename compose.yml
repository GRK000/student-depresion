name: mlops

services:
  api:
    build:
      context: .
      target: production
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
    restart: unless-stopped
